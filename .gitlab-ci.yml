# image: google/cloud-sdk:slim //try cicd
image: google/cloud-sdk:slim

services: []

default:
  tags:
    - adnex-bi

stages:
  - build
  - migrate
  - deploy

# -------- Project variables --------
variables:
  DOCKER_TLS_CERTDIR: ""
  GCP_PROJECT_ID: adnex-product
  GCP_REGION: asia-east1
  IMAGE_DEV: "gcr.io/$GCP_PROJECT_ID/$CI_PROJECT_NAME-dev:$CI_COMMIT_SHA"
  IMAGE_PROD: "gcr.io/$GCP_PROJECT_ID/$CI_PROJECT_NAME:$CI_COMMIT_SHA"
  CLOUD_RUN_SERVICE: "adnex-bi-be"
  SERVICE_ACCOUNT: "giant-wang-kdanmobile-com@adnex-product.iam.gserviceaccount.com"

# 只在 dev 分支與「版本標籤」建立 pipeline（避免其它分支誤觸發）
workflow:
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'
    - when: never

# 共用前置：安裝 docker CLI + gcloud 認證
.before_gcloud_and_docker: &before_gcloud_and_docker
  - apt-get update && apt-get install -y docker.io git
  - gcloud auth revoke --all || true
  - echo "$GCP_SERVICE_ACCOUNT_KEY" | base64 -d > gcp-key.json
  - gcloud auth activate-service-account --key-file=gcp-key.json --quiet
  - gcloud config set project "$GCP_PROJECT_ID"
  - gcloud auth configure-docker gcr.io --quiet

# ========== BUILD ==========
build:
  stage: build
  before_script: *before_gcloud_and_docker
  script:
    - docker version
    - |
      if [ "$CI_COMMIT_BRANCH" = "dev" ]; then
        docker build -t "$IMAGE_DEV" .
        docker push "$IMAGE_DEV"
      else
        docker build -t "$IMAGE_PROD" .
        docker push "$IMAGE_PROD"
      fi
  rules:
    # dev 分支一定跑
    - if: '$CI_COMMIT_BRANCH == "dev"'
    # 發版標籤也要跑（產 prod 用映像）
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'

# ========== MIGRATE ==========
migrate:
  stage: migrate
  needs: [build]
  before_script: *before_gcloud_and_docker
  script:
    - docker pull "$IMAGE_DEV"
    - |
      docker run --rm \
        --entrypoint /bin/sh \
        -e DATABASE_URL="$DATABASE_URL" \
        "$IMAGE_DEV" \
        -c 'echo ">>> Inside container DATABASE_URL = $DATABASE_URL"; alembic upgrade head'
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'

# ========== DEPLOY: DEV ==========
deploy:dev:
  stage: deploy
  needs: [build, migrate]
  before_script: *before_gcloud_and_docker
  script: |
    gcloud run deploy "${CLOUD_RUN_SERVICE}-dev" \
      --region "$GCP_REGION" \
      --platform managed \
      --image "$IMAGE_DEV" \
      --service-account "$SERVICE_ACCOUNT" \
      --allow-unauthenticated \
      --quiet
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'

# ========== DEPLOY: PROD ==========
deploy:
  stage: deploy
  needs: [build, migrate]
  before_script: *before_gcloud_and_docker
  script: |
    git fetch origin main
    if git merge-base --is-ancestor "$CI_COMMIT_SHA" origin/main; then
      gcloud run deploy "${CLOUD_RUN_SERVICE}" \
        --region "$GCP_REGION" \
        --platform managed \
        --image "$IMAGE_PROD" \
        --service-account "$SERVICE_ACCOUNT" \
        --allow-unauthenticated \
        --quiet
    else
      echo "Tag is not from main, skipping deploy."
      exit 1
    fi
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'
